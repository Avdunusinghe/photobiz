// Site-wide JavaScript for the tenant portfolio site. Gallery-template-specific behavior
// (Swiper/GLightbox init) lives inline in Views/Galleries/Details.cshtml instead, since it depends
// on which template that specific gallery uses.
(function () {
  'use strict';
})();
